# Portal — 测试计划文档

> 本文档定义 Portal 个人网站平台的全系统测试策略，覆盖单元测试、集成测试、E2E 测试及性能/安全测试。

---

## 1. 测试策略总览

### 1.1 测试金字塔

```
          ┌────────────┐
          │   E2E 测试   │  少量，覆盖关键用户流程
          │  Playwright  │
          ├────────────┤
          │  集成测试     │  中量，验证模块间协作
          │  Vitest+DB   │
          ├────────────┤
          │  单元测试     │  大量，覆盖工具函数/组件
          │   Vitest     │
          └────────────┘
```

### 1.2 技术选型

| 工具 | 用途 |
|------|------|
| **Vitest** | 单元测试 + 集成测试（与 TypeScript/ESM 原生兼容） |
| **React Testing Library** | React 组件测试 |
| **Playwright** | E2E 浏览器测试 |
| **MSW** (Mock Service Worker) | API Mock（前端隔离测试） |
| **Faker.js** | 生成测试数据 |
| **Lighthouse CI** | 性能回归测试 |

### 1.3 覆盖率目标

| 层级 | 覆盖率目标 | 说明 |
|------|-----------|------|
| 工具函数 (`packages/shared`) | ≥ 90% | 纯逻辑，必须高覆盖 |
| tRPC Routers (`packages/api`) | ≥ 80% | 核心业务逻辑 |
| React 组件 | ≥ 60% | 重点覆盖交互组件 |
| E2E 流程 | 覆盖所有关键路径 | 不追求行覆盖率 |

---

## 2. 单元测试

### 2.1 packages/shared — 工具与类型

| 测试项 | 用例 |
|--------|------|
| 类型导出 | 所有类型可正确导入，无编译错误 |
| 工具函数（后续添加） | slug 生成、日期格式化、字符串截断等 |

### 2.2 packages/config — 配置系统

| 测试项 | 用例 |
|--------|------|
| `defineConfig()` | 输入完整配置 → 原样返回 |
| 预设加载 | 每个 preset 名称 → 返回正确的模块列表 |
| 模块覆盖 | preset + 手动 modules 覆盖 → 合并正确 |
| 无效配置 | 缺少必填字段 → 抛出 Zod 校验错误 |

### 2.3 packages/theme — 主题系统

| 测试项 | 用例 |
|--------|------|
| 主题数据完整性 | 每套主题包含所有必填 Token（colors, typography, spacing, effects） |
| ThemeProvider | 渲染子组件、注入正确 CSS 变量 |
| useTheme Hook | 初始主题读取、切换主题、localStorage 持久化 |
| 暗色模式适配 | `prefers-color-scheme: dark` → 自动选择暗色主题 |

### 2.4 packages/db — 数据库

| 测试项 | 用例 |
|--------|------|
| Prisma Schema | `prisma validate` 通过，无 schema 错误 |
| 迁移一致性 | `prisma migrate diff` 无 drift |
| Singleton Client | 多次 import 返回同一实例 |

### 2.5 packages/api — tRPC Routers

> 使用内存数据库或 test container 进行集成级单元测试。

| Router | 测试用例 |
|--------|----------|
| **post** | `list` 分页正确、`bySlug` 返回正确文章、`create` 需要 auth、`update` 只允许 author/admin |
| **category** | `list` 返回所有分类、`create` 名称唯一约束 |
| **comment** | `create` 关联到 post、嵌套 reply、`delete` 需要 admin |
| **guestbook** | `list` 时间倒排、`create` 速率限制 |
| **project** | `list` 按 sortOrder 排序、`create/update/delete` 需要 auth |
| **media** | `upload` 文件类型校验、大小限制 |
| **link** | `list` 按分类分组、`checkAlive` 正确检测 URL 可达性 |
| **pageView** | `record` 正确写入、`stats` 聚合计算正确 |
| **siteConfig** | `get/set` CRUD、值为 JSON 正确序列化 |

---

## 3. 集成测试

### 3.1 数据库集成

| 测试场景 | 验证点 |
|----------|--------|
| Post 创建完整流程 | 创建 Category → 创建 Post → 关联 Tags → 查询验证关系 |
| 级联删除 | 删除 Post → 关联的 Comment、PostTag 一并删除 |
| 唯一约束 | 重复 slug/email → 抛出正确错误 |
| 事务回滚 | 部分操作失败 → 整体回滚 |

### 3.2 API + Auth 集成

| 测试场景 | 验证点 |
|----------|--------|
| 未认证访问管理 API | 返回 401 |
| viewer 角色访问写入 API | 返回 403 |
| admin 创建文章 | 返回 201 + 正确数据 |
| Token 过期 | 返回 401 + refresh 逻辑 |

### 3.3 搜索集成

| 测试场景 | 验证点 |
|----------|--------|
| 文章发布 → 索引同步 | MeiliSearch 可搜到新文章 |
| 文章删除 → 索引清理 | 搜索不再返回已删除文章 |
| 中文搜索 | 中文分词结果正确 |
| 模糊搜索 | 错别字 / 拼音 → 返回相关结果 |

---

## 4. E2E 测试 (Playwright)

### 4.1 测试环境

```bash
# 启动测试环境
docker compose -f docker-compose.test.yml up -d
pnpm db:migrate
pnpm db:seed
pnpm build
pnpm start &
pnpm test:e2e
```

### 4.2 关键用户流程

#### 访客流程

| 编号 | 流程 | 步骤 | 预期结果 |
|------|------|------|----------|
| V-01 | 浏览博客 | 首页 → 博客列表 → 点击文章 → 阅读 | 文章正确渲染，目录可用 |
| V-02 | 文章评论 | 文章页 → 填写评论 → 提交 | 评论出现在列表中 |
| V-03 | 分类筛选 | 博客列表 → 点击分类标签 → 筛选 | 仅显示该分类文章 |
| V-04 | 全站搜索 | Cmd+K → 输入关键词 → 点击结果 | 跳转到对应页面 |
| V-05 | 留言板 | 留言板页面 → 填写留言 → 提交 | 留言出现在列表中 |
| V-06 | 主题切换 | 点击主题切换器 → 选择主题 → 刷新页面 | 主题保持，样式正确 |
| V-07 | 响应式 | 缩小窗口至移动端宽度 | 汉堡菜单出现，布局正确 |
| V-08 | 关于我 | 导航 → 关于我页面 | 个人信息、社交链接正确 |
| V-09 | 作品集 | 作品集列表 → 筛选 → 详情 | 项目信息正确显示 |
| V-10 | RSS | 访问 /api/feed.xml | 返回有效 RSS XML |

#### 管理员流程

| 编号 | 流程 | 步骤 | 预期结果 |
|------|------|------|----------|
| A-01 | 登录 | /admin → 登录页 → 输入凭据 → 登录 | 跳转到仪表盘 |
| A-02 | 发表文章 | 内容管理 → 新建 → 编写 Markdown → 发布 | 前台可见新文章 |
| A-03 | 编辑文章 | 文章列表 → 编辑 → 修改 → 保存 | 前台更新内容 |
| A-04 | 草稿管理 | 新建草稿 → 预览 → 发布 | 草稿不在前台显示，发布后可见 |
| A-05 | 媒体上传 | 媒体库 → 上传图片 | 图片可在编辑器中引用 |
| A-06 | 评论审核 | 仪表盘 → 评论列表 → 批准/删除 | 审核后评论状态更新 |
| A-07 | 主题切换 | 外观配置 → 切换主题 → 预览 → 应用 | 前台主题更新 |
| A-08 | 模块管理 | 模块管理 → 禁用博客模块 | 前台博客入口消失 |
| A-09 | 仪表盘数据 | 仪表盘页面 → 检查统计卡片 | 数据与实际一致 |
| A-10 | 未授权访问 | 未登录直接访问 /admin/dashboard | 重定向到登录页 |

### 4.3 跨浏览器测试

| 浏览器 | 版本 | 优先级 |
|--------|------|--------|
| Chromium | latest | 🔴 必测 |
| Firefox | latest | 🟡 推荐 |
| WebKit (Safari) | latest | 🟡 推荐 |
| Mobile Chrome | latest | 🟡 推荐 |
| Mobile Safari | latest | 🟡 推荐 |

---

## 5. 性能测试

### 5.1 Lighthouse CI

```yaml
# lighthouserc.json
{
  "ci": {
    "collect": {
      "url": ["http://localhost:3000", "http://localhost:3000/blog"],
      "numberOfRuns": 3
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.9 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }]
      }
    }
  }
}
```

### 5.2 Core Web Vitals 目标

| 指标 | 目标 | 说明 |
|------|------|------|
| **LCP** (Largest Contentful Paint) | < 2.5s | 博客列表/详情页 |
| **FID** (First Input Delay) | < 100ms | 主题切换、搜索交互 |
| **CLS** (Cumulative Layout Shift) | < 0.1 | 图片/字体加载不跳动 |
| **TTFB** (Time to First Byte) | < 200ms | SSG 页面 |
| **Bundle Size** | < 200KB (首屏 JS) | 使用 bundle-analyzer 监控 |

### 5.3 负载测试（可选）

| 场景 | 工具 | 目标 |
|------|------|------|
| 并发访问博客列表 | k6 | 100 并发，p99 < 500ms |
| 并发提交评论 | k6 | 50 并发，无数据丢失 |
| 搜索并发 | k6 | 50 并发，p95 < 200ms |

---

## 6. 安全测试

| 测试项 | 方法 | 预期 |
|--------|------|------|
| XSS 注入 | 评论/留言输入 `<script>alert(1)</script>` | 脚本不执行，被转义显示 |
| SQL 注入 | API 参数注入 `'; DROP TABLE posts;--` | Prisma 参数化查询阻止 |
| CSRF | 跨域直接 POST 管理 API | 被 CSRF Token / SameSite 阻止 |
| 越权访问 | viewer 角色调用 admin API | 返回 403 |
| 速率限制 | 1 秒内提交 100 次评论 | 触发限流，返回 429 |
| 敏感信息泄露 | 检查 API 响应是否包含密码/token | 不包含任何敏感字段 |
| 文件上传 | 上传 .exe / .sh 文件 | 被文件类型白名单拒绝 |
| JWT 伪造 | 修改 JWT payload 后调用 API | 返回 401 |

---

## 7. 可访问性测试

| 测试项 | 工具 | 标准 |
|--------|------|------|
| WCAG 2.1 AA | axe-core / Lighthouse | 0 violations |
| 键盘导航 | 手动测试 | Tab 可遍历所有交互元素 |
| 屏幕阅读器 | VoiceOver (Mac) | 内容可正确朗读 |
| 色彩对比度 | Lighthouse | 所有文本对比度 ≥ 4.5:1 |
| 焦点可见 | 手动测试 | 焦点环始终可见 |

---

## 8. 按阶段测试计划

| Phase | 新增测试 | 侧重点 |
|-------|----------|--------|
| **Phase 1** | 主题引擎单元测试、布局组件测试 | ThemeProvider、响应式断点 |
| **Phase 2** | tRPC Router 集成测试、E2E 访客流程 (V-01~V-06) | DB 全链路、博客 CRUD |
| **Phase 3** | 搜索集成测试、Auth 集成测试、E2E 管理员流程 (A-01~A-06) | 认证鉴权、搜索准确性 |
| **Phase 4** | 作品集/统计模块测试、E2E (A-07~A-09) | 管理后台完整流程 |
| **Phase 5** | 各扩展模块单元测试 | 模块隔离性 |
| **Phase 6** | Lighthouse CI、安全测试、跨浏览器 E2E | 性能/安全回归、生产就绪 |

---

## 9. CI 集成

```yaml
# .github/workflows/test.yml
name: Test
on: [push, pull_request]

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test --coverage

  e2e:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env: { POSTGRES_DB: portal_test, POSTGRES_USER: portal, POSTGRES_PASSWORD: test }
        ports: ["5432:5432"]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:migrate
      - run: pnpm build
      - run: pnpm exec playwright install --with-deps
      - run: pnpm test:e2e

  lighthouse:
    runs-on: ubuntu-latest
    needs: [e2e]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install --frozen-lockfile
      - run: pnpm build && pnpm start &
      - run: pnpm exec lhci autorun
```

### 质量门禁（PR 合并条件）

- ✅ 单元/集成测试全部通过
- ✅ TypeScript 编译无错误
- ✅ Biome lint 无错误
- ✅ E2E 关键路径全部通过
- ✅ Lighthouse 四项评分 ≥ 90
- ✅ 无新增安全漏洞（依赖审计）
